provider "aws" {
  region = "ap-south-1"
}

resource "aws_instance" "web" {
  ami           = "ami-0f5ee92e2d63afc18"
  instance_type = "t3.micro"
  key_name      = "mykey"

  vpc_security_group_ids = ["sg-XXXXXXXX"]   # (abhi blank chhod de, next step me fill karenge)

  user_data = <<EOF
#!/bin/bash
yum install -y httpd php
systemctl start httpd
systemctl enable httpd
cd /var/www/html
echo "<h1>Dhruv 3 Tier App Running</h1>" > index.php
EOF

  tags = {
    Name = "Dhruv-Web"
  }
}
